version: "3.7"
services:
  teamu-front:
    build:
      context: ./
    environment:
      - 'RUN_ENV=development'
    env_file: ./client.env
    volumes:
      - ./:/home/node/client
    ports:
      - 1000:3000
      - 1001:5001

networks:
  default:
    name: ${DOCKER_NETWORK}
